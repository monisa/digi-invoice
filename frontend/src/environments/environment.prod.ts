// Production environment. On Hostinger, point this at the API subdomain, e.g.
// https://api.yourapp.com/api/v1 (set per the deployment domain).
export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
};
